{
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "npm install --save-dev @types/dom-speech-recognition && npm run build"
      }
    }
  ]
}
